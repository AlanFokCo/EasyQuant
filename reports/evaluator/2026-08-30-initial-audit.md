# eqlib 对抗性审查：初始基线

审查时间：2026-08-30；环境：macOS arm64、CPython 3.10.20；profile：`offline --strict`。

## 结论

严格离线 evaluator 返回 **0 个 P0/P1 发现**。配套机器可读证据见同目录的 `2026-08-30-initial-audit.json`。

## 本轮已关闭的高风险问题

- 腾讯行情适配器曾把价格缩小 100 倍、金额按错误单位换算，并未裁剪请求日期；现已按原始价格、正确金额与请求区间校验。
- 交易日 fallback 曾把法定工作日当作交易日；现使用打包的 A 股交易日资源，覆盖范围外可显式严格失败。
- 组合风控曾忽略回撤阈值、VaR 未按净值加权；现按组合净值与回撤阈值执行。
- 统计下行偏差和 ML 推理曾依赖批次或错误概率列；现使用 LPM2 定义、训练期填充值和明确的正类索引。
- 运行时依赖、wheel 元数据和 Python 3.10 四目标哈希锁已由 evaluator 交叉审查。

## 审查边界

本次证据是确定性的离线审查。真实数据提供方的调用单独由 `live` profile 运行，受 90 秒总时限保护；提供方不可用会以 `DATA-190 / unavailable` 留痕，而不作为离线通过的证据。

性能基线仅在受控的 macOS Python 3.10 环境比较冷导入时间与 RSS；其它平台会保留 P2 的“基线不可用”证据，而不会跨平台错误比较。
